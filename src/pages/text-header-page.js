import React from "react"
import TextOnlyHeader from "../components/textOnlyHeader"

export default () => (
  <div style={{ color: `teal` }}>
    <TextOnlyHeader headerText="This is a Text-Only Header." />
    <p>This is a text only header component with no background, borders, or styling aside from text color.</p>
  </div>
)



